<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ibrajix.prime">

    <application
        android:name=".hilt.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_prime"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_prime_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prime">
        <activity
            android:name=".ui.activities.IntroActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>